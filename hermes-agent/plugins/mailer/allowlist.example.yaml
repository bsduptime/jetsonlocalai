# Per-recipient send authorization.
# Edit this file to add or remove people the Hermes agent is allowed to
# email. The agent CANNOT exceed `daily_limit` per contact per local day.
# Changes are picked up on the next tool call — no restart needed.
#
# Path: ~/.hermes/email-plugin/allowlist.yaml
# Mode: 600. ACL u:dbexpertai:rw so David can edit without sudo.

contacts:
  # - email: alice@example.com
  #   daily_limit: 5
  #   note: "personal friend"
  # - email: bob@example.com
  #   daily_limit: 2
  #   note: "work colleague — keep it sparse"
